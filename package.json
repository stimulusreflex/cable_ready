{
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "package"
    ]
  },
  "scripts": {
    "lint": "yarn workspace cable_ready run lint",
    "build": "yarn workspace cable_ready run build",
    "build:doc": "yarn workspace cable_ready run build:doc",
    "watch": "yarn workspace cable_ready run watch",
    "start": "yarn workspace cable_ready run start",
    "prerelease": "yarn build",
    "release:beta": "yarn workspace cable_ready run release:beta",
    "clean": "yarn workspace cable_ready run clean"
  }
}
